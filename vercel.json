{

    "version": 2,
    "builds": [
    {
      "src": "api/**/*.py",
      "use": "@vercel/python@canary"
    }
  ],
  "scripts": {
    "build": "pipenv install"
  },
    "rewrites": [
      { "source": "/(.*)", "destination": "api/index.py" }
    ]

  }